/**
 * Timeout with promise
 * @param {int} ms
 * @param {function=} cb
 * @return {Promise}
 */
function timeout(ms, cb) {
    return new Promise(resolve => setTimeout(() => {cb && cb(); resolve(); }, ms))
}

module.exports = timeout;
